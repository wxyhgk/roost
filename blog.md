# 我把终端搬进了浏览器：roost

## 1. 前言

先说结论：**roost 是一个跑在浏览器里的终端工作台。会话活在服务器上，你换一台设备打开网页，终端还停在你离开的那一行。** 源码在 GitHub：<https://github.com/wxyhgk/roost>。

下面是我为什么要自己写一个。

用 AI 写代码已经是每个独立开发者的必备技能了。大多数时候我们用 Codex CLI、Claude Code 这类命令行工具，或者对应的桌面端，这么开发本身没什么问题。

问题出在**终端和设备是绑定的**。

关掉终端、断开 SSH、换一台电脑，往往意味着你要重新找回原来的目录、进程和 agent 会话。`tmux` 能让进程活下来，但跨设备访问和恢复工作状态依然不自然——而且它不知道终端里跑的是什么。

为了解决这件事，我折腾了很久。

### 1.1 VSCode：状态保不住

可以在自己的电脑上起一个 web 版，然后从网页连过去。但在线 VSCode 有个要命的毛病：**它保不住"状态"**。你打开了某个终端、开了某个窗口，过一段时间它就把这些收回去了，于是你又得重新找文件夹。远程开发最怕的就是这个——每次回来都要重新铺一遍摊子。

### 1.2 JupyterLab：能用，但跟不上了

因为 VSCode 保不住状态，我有很长一段时间是用 JupyterLab 做远程开发的，还用 AI 写了一堆插件来补痛点：右键跳到终端所在的文件夹、改终端标签页的名字，等等。勉强能用。

但 JupyterLab 已经跟不上现在的开发方式了。AI coding 的时候，我们真正需要的是这样一个三段式结构：

```
项目/会话        终端/对话         文件
```

现在主流的 AI 编程工具基本都是这个形状，ChatGPT 和 Claude 的桌面端都是。我试过改 JupyterLab 的源码去凑这个结构，结果是**根本改不下去**——历史包袱太重，改一处冒出三个 bug。

### 1.3 市面上的 web 端：要么没终端，要么全是折叠的对话框

市面上绝大多数在做桌面端，web 端本来就少。即便有——比如 DeepSeek harness 这类 web 应用——它不做终端，也不做文件树。有文件树的，用的基本也是 GUI 对话框那一套。

GUI 对话框看起来很好：改了哪些代码、跑了哪条命令，统统折叠起来，界面干净。**但人是懒的。** 折叠起来的东西，大多数时候根本不会去点开。而实际开发里我认为很有必要看它到底怎么写的、具体写了什么代码。

所以我认为 **TUI 没有过时**。而且 TUI 更省内存、更快。我那台 64G 的 Mac 被 ChatGPT 桌面端吃掉 80G 内存直接卡死过，对话一长就卡得没法用，真是离谱。

### 1.4 延迟：那 mosh 不就够了吗

远程开发还有个绕不开的问题是延迟。我连美国的服务器，往返至少 200ms 起步，打一个字要等两百毫秒才看见，根本没法用。

懂行的人这时候会说：mosh 不就解决了吗。

mosh 确实做了本地回显，但它解决的只是**一条 SSH 连接的手感**。它给不了你浏览器里的文件树、笔记、订阅额度、agent 状态；更实际的是，别人的电脑上、iPad 上没有 mosh 客户端，**但都有浏览器**。

### 1.5 于是自己写

折腾了一圈之后我决定自己写一个，源码和设计全自己定，以后要改也方便。就有了 roost。

**先把边界说清楚**：它是**单人本机**工具。数据都在 `~/.roost/`，密码只防局域网里的其他人，**登录就等于能在你的机器上跑命令**。它不是多租户服务，也没做云同步和协作——不要把它暴露到公网上。

## 2. roost 长什么样

### 2.1 三段式

界面参考了 [warp](https://github.com/warpdotdev/warp) 的思路，分三段。

**左侧**是工作区和会话列表。每一条会话带着它自己那个 AI CLI 的 logo，正在跑的那条会标出来——不用点进去就知道哪个在忙。目前认得 Codex、Claude Code、Qwen Code、Gemini CLI、Grok、Oh My Pi、OpenCode 七家。

![左侧会话列表](image.png)

**中间**是终端本体。打开某个 CLI 的会话时，背景会浮出它的 logo 水印，一眼知道自己在哪个窗口里。

**右侧**是文件树、笔记、代码片段、任务清单、进程列表、服务器状态。

![右侧面板](image.png)

**底部状态栏**放的是一眼就想看到的东西：CPU、内存、上下行速度，以及 Claude / Codex / OpenCode 订阅额度的实时读数——不用切出去查还剩多少。

![底部状态栏](image.png)

### 2.2 为延迟做的事：本地回显

打字的时候，字是**当前这个浏览器**直接画出来的，不等服务器；只有真正发出去的那一下才有延迟。200ms 的链路上，手感和本地几乎没差别。

但"做了本地回显"这句话谁都能说，**有意思的是它凭什么敢信**。

roost 画的不是"已经发生的事"，是**预测**。每一次预测都要等真实回声来对账：

- 对上了，继续预测；
- **对不上、屏幕发生了别的变化、或者超时——整份预测当场丢掉**，回到服务器说的那一屏。

所以它不会骗你。遇到那种根本不回显的提示符（比如输密码），它自己就退场了，不会在屏幕上留下一串你以为输进去了的字符。

一个会骗人的本地回显比没有更糟，所以宁可退场也不猜。

## 3. 几个设计上的选择

功能列表谁都能堆，下面这几个是我觉得真正把 roost 和别的东西分开的地方。

### 3.1 不折叠

前面骂过 GUI 把一切折叠起来。roost 的做法是反过来的：**终端里显示什么，就是什么**，没有第二层"摘要"挡在中间。你看到的就是 agent 真正写的代码、真正跑的命令。

### 3.2 文件用弹窗，不停靠

这是整个项目的设计哲学：**专注**。

文件、笔记、预览一律以弹窗形式浮出来，不是钉死在终端旁边占掉半个屏幕。看完关掉，终端还是整块的。

但"只能看一个"也不对——很多时候你就是要对着两个文件看。所以钉住之后可以接着开下一个：**钉住的窗口从"当前选中"上摘下来，那个位置空出来给下一个文件**。想同时看三个就钉三个，级联排开，点哪个哪个到最上面。规则只有这一条，没有平铺、没有最小化、没有记住布局——那几样都要先猜你想怎么排，而我没有依据可猜。

### 3.3 agent 状态不靠读屏幕

"agent 现在在干嘛"这件事，很多工具是去**读屏幕**猜出来的——看看有没有转圈的动画、有没有新的输出。

问题是：**"正在安静地算"和"弹了确认框在等你"，在输出流上长得一模一样**，都是不再产生输出。靠猜分不开这两件事，而这两件事对你的意义完全相反：一个是不用管，一个是你不回它就一直卡着。

所以 roost 走的是另一条路：让 agent **自己上报**。通过注入的插件（以及 codex 的 app-server）拿到结构化事件——会话开始、提交了提示词、请求权限、问了问题、跑完了。于是"在忙"和"在等你"分得清清楚楚，会话列表上直接标出来。

### 3.4 Ctrl+C：小处见功夫

这件事我觉得最能说明 roost 想做成什么样。

在 shell 里，`Ctrl+C` 的意思是"这行不要了"，代价只有一个新的提示符。而 agent CLI 把同一个键接到了"打断这一轮"上。于是同一个手指动作的代价，从"少打一行字"变成了"**刚才那几分钟白跑了**"。

两边都没错，但手指只有一套记忆。模型跑着的时候你想清空输入框，随手一个 `Ctrl+C`，模型就停了。

roost 的处理是：**agent 正在跑的时候，第一下 `Ctrl+C` 先替你清空输入框**，并提示"再按一次 Ctrl+C 打断模型"；1.5 秒内再按一下才真的打断。这不是我发明的交互——那些 CLI 自己对"Ctrl+C 退出"就是这么做的。

关键在于**清空键是量出来的，不是猜的**。我写了个探针在真 PTY 里跑：打一串标记 → 按 `Ctrl+U` → 再打一串 → 看最后那行剩下什么。claude、Oh My Pi、opencode 都量到了；codex 因为是整屏重绘，这个法子量不出来，**那就留空不拦**，`Ctrl+C` 原样发下去。

猜一个键的代价是不对称的：猜对了省一次误打断，猜错了是往一个正在跑的 agent 里塞一个谁也不知道会触发什么的控制字符。**没量出来就不做。**

## 4. 一些顺手的东西

- **笔记**：AI 输出里有值得留的段落，选中直接存成笔记，不用复制粘贴倒腾到别处。
- **任务清单钉在侧栏**：agent 列的 todo 在终端里会随着输出滚上去，跑十几分钟之后早就找不着了。侧栏不滚。
- **拖文件夹上传**：整个文件夹直接拖进文件树，递归展开，目录结构原样建出来。
- **跨设备恢复**：关掉标签页、换台电脑，重新打开网页，终端还在原地——这是它一开始就要解决的那件事。

## 5. 最后

roost 在 GitHub 开源：<https://github.com/wxyhgk/roost>

它借鉴了很多项目——界面结构上的 warp、agent 事件协议、以及一堆同类工具踩过的坑。集百家之长，也做了一些我认为别人没做对的取舍。

如果你也在远程做 AI coding，也受够了状态保不住、延迟磨人、GUI 把一切折叠起来，欢迎试试，也欢迎提 issue。
